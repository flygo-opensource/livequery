# Changelog — @livequery/expo-sqlite

## 3.2.0

### Added
- `LivequerySQLiteStorage`: a persistent `LivequeryStorage` on expo-sqlite for React Native / Expo. Documents, pending writes, the outbox and the sync's bookkeeping survive an app restart. It passes the storage conformance suite of `@livequery/client/testing`.
