/** Where services and gateways meet: same namespace and tag, and the same key (`SIMPLE_DISCOVERY_KEY`). */
export const DISCOVERY_NAMESPACE = 'livequery'
export const DISCOVERY_TAG = 'livequery-service'
