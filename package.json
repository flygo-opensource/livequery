{
  "name": "livequery-workspace",
  "private": true,
  "packageManager": "bun@1.2.5",
  "workspaces": [
    "packages/*",
    "examples",
    "examples/cf-worker",
    "examples/todo-app",
    "examples/todo-offline",
    "examples/chat-demo"
  ],
  "scripts": {
    "build": "bun scripts/build-packages.ts && bun run --cwd examples typecheck && bun run --cwd examples/cf-worker type-check",
    "test": "bun scripts/test-packages.ts && bun run --cwd examples test"
  }
}
